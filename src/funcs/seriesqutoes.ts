import { NumberSeries } from "../core/series";

const OPEN = new NumberSeries([])
const O = OPEN

const CLOSE = new NumberSeries([])
const C = CLOSE

const LOW = new NumberSeries([])
const L = LOW

const HIGH = new NumberSeries([])
const H = HIGH

const VOLUMN = new NumberSeries([])
const V = VOLUMN
const VOL = VOLUMN


const AMOUNT = new NumberSeries([])
const AMO = AMOUNT



